#include "main.h"

/**
 * add - Entry point
 * @a: a is an ascii characters
 * @b: b is an ascii character
 *
 * Description: adds two integers
 *
 * Return: 0 (success)
 */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
