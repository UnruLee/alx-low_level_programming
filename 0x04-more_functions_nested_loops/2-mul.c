#include "main.h"

/**
 * mul - Entry point
 * @a: ascii character for variable
 * @b: ascii character for variable
 *
 * Description: multiply two integers
 *
 * Return: Always 0
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	_putchar(prod);

	return (0);
}
