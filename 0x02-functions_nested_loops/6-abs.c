#include "main.h"

/**
 * _abs - Entry point
 * @int: int a data type
 *
 * Description: prints absolute value
 *
 * Return: 0 (success)
 */

int _abs(int)
{
	int n;

	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);
	else
		return (n);
}
